VITE_API_BASE_URL=http://localhost:3001
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_12345

# Cloudflare R2 / CDN base for heavy static assets (GLBs, thumbnails, table textures).
# Leave unset for local dev (assets served from the API's /uploads and /public).
# In production set to your R2 custom domain, e.g. https://assets.yourdomain.com
# VITE_ASSET_BASE_URL=https://assets.yourdomain.com
